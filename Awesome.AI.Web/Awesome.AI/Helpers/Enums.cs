namespace Awesome.AI.Helpers
{
    public class Enums
    {
        public enum MINDS { ROBERTA, ANDREW }
        public enum THECHOISE { YES, NO, UNDEFINED }
        public enum TYPE { JUSTAUNIT, LEARNING, PERSUE, IDLE, NOTNEXTPREV }
        public enum DIRECTION { NONE }
        public enum FUZZYNO { VERY_SOFT, SOFT, MID, HARD, VERY_HARD, MIN }
        public enum FUZZYCHANCE { VERY_LIKELY, LIKELY, MID, UNLIKELY, VERY_UNLIKELY }
        public enum OPINION { OK, NOT, NONE }
        public enum SENTENCECHOOSER { NONE, IS, ISQ, LIKE, WANT }
        public enum VALIDATION { BOTH, EXTERNAL, INTERNAL }
        public enum TAGSETUP { PRIME, OTHER }
        public enum TAGS { ALL, EVEN }
        public enum OCCUPASION { FIXED, DYNAMIC }
        public enum OUTPUT { MOM, POS, NOTUSED }
        public enum TYPELIMIT { SIMPLE, SIGMOID, CHANCE }
        public enum MECHANICS { NONE, GRAVITY, CONTEST, WHEEL, HILL }
        public enum HACKMODES { HACK, NOHACK }
        public enum MATRIX { SIMPLE, GPT }
    }
}
